
- show images in balls
    . gradually change from image to small color ball as chart gets busier

- filters
    - category (on request)
    - listing type (on chart)
        - add item filters in general to chart
    - listing end
    - ships to
    - price (zoom on chart)

- ui
    - collapse/expand aspects
    ? change aspect filters according to selected category (hide unused ones)
    - change x-axis from filter titles
    - config
        - items per request
        - disable guesses
    - colors
       http://coolors.co/browser
       http://www.colorcombos.com/color-schemes/7625/ColorCombo7625.html

- usability
    ? list undo select w/o ctrl
        . bad for request on double click though

- legend
    - insert in order when new values appear

- proper starting screen/sequence
    . no empty legend
    . no need to click request twice

- bugs
    - tooltip get stuck
    - sync requests get status animation stuck

- ebay api wrapper
    https://github.com/benbuckman/nodejs-ebay-api
    . needs node.js?
    . node.js for web requires "express"?
    . no problem w/ "same origin policy"?
