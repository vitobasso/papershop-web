- refactor
    - extract ebay dependant stuff
    - add ml
    - add others? marktplaats, olx, etc

- functionality
    - filters
        - to create
            - ships to
            - price (zoom on chart)
            - seller
            - seller score
        - show currently applied filters in request-log dialog
        ? change aspect filters according to selected category (hide unused ones)

    - config
        - ebay site, "AvailableTo" country
        ? items per request
        - disable guesses

    - fetch items when click x axis label (add that label's value on respective filter. remove other values selected for same filter if any)
    - hide unrelated aspects when category is selected

- messages
    - empty keywords
    - errors

- bugs
    - tooltip get stuck
    - category selection lost when collapsing list

- ui
    - colors
       http://coolors.co/browser
       http://www.colorcombos.com/color-schemes/7625/ColorCombo7625.html

- ebay api wrapper
    https://github.com/benbuckman/nodejs-ebay-api
    . needs node.js?
    . node.js for web requires "express"?
    . no problem w/ "same origin policy"?
