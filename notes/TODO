- functionality

    - show full sources > categories > aspects > aspect values (dev purposes)

    - infer "same as"
        - categories of different sources (and respective aspect)
        - show fused categories/aspects when multiple categories/sources are selected

    - show inferences/guesses
        - show what was guessed by the app: fused categories, item's aspect values
        - allow feedback (but do what with it? store as local preferences?)

    - filters
        - to create
            - ships to
            - price (zoom on chart)
            - seller
            - seller score
        - show currently applied filters in request-log dialog

    - config
        - ebay site, "AvailableTo" country
        ? items per request
        - disable guesses

    - fetch items when click x axis label (add that label's value on respective filter. remove other values selected for same filter if any)

- messages
    - empty keywords
    - errors

- bugs
    - tooltip get stuck

- ui
    - colors
       http://coolors.co/browser
       http://www.colorcombos.com/color-schemes/7625/ColorCombo7625.html

- ebay api wrapper
    https://github.com/benbuckman/nodejs-ebay-api
    . needs node.js?
    . node.js for web requires "express"?
    . no problem w/ "same origin policy"?
